used liquidglass, turns out it only supports chromium browsers
so i made a pseudo-liquid-glass fallback for firefox, mobile devices, and safari

other than that, it's just a portfolio site with a nice look (because i can)